#!/bin/sh
set -e

cat > /data/valkey.conf <<EOF
appendonly yes

user default off
user ${BELABERUNG_VALKEY_USER} on >${BELABERUNG_VALKEY_PASS} ~* +@all -@dangerous
EOF

exec valkey-server /data/valkey.conf